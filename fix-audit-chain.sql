-- ============================================================
-- ConSecOrg — Сброс хэш-цепочки аудита
-- ============================================================
-- ПРИЧИНА: Записи, созданные до фикса (commit 2d18690),
-- содержат CurrentHash, посчитанный с ПОЛНОЙ точностью
-- timestamp (7 знаков, напр. "...1234567Z"), тогда как
-- верификация теперь усекает до мс ("...1230000Z").
-- Эти записи НЕЛЬЗЯ исправить — их нужно удалить.
--
-- КОГДА ЗАПУСКАТЬ:
--   Один раз, перед первым входом в систему после обновления.
--   После запуска создайте свежие записи через логин.
-- ============================================================

USE ConSecOrg;   -- поменяйте на имя вашей БД если отличается
GO

-- Сколько записей будет удалено (проверка)
SELECT COUNT(*) AS records_to_delete FROM audit_logs;
GO

-- Удалить ВСЕ записи аудита
DELETE FROM audit_logs;
GO

-- Сбросить счётчик IDENTITY SequenceNum (опционально)
DBCC CHECKIDENT ('audit_logs', RESEED, 0);
GO

SELECT 'Хэш-цепочка сброшена. Записей в таблице: ' + CAST(COUNT(*) AS NVARCHAR) AS result
FROM audit_logs;
GO
